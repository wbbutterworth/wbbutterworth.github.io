import style from 'style';

const width  = 40;
const height = 40;

const styles = {
	backgroundColor: style['highlight-color'],
	top:        height / 2,
	left:       width / 2,
	height,
	width,
};

export default styles;
